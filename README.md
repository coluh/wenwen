
to run:

```Shell
sudo apt install libopenblas0 libgfortran5
git clone https://huggingface.co/Qwen/Qwen2.5-0.5B
make run
```

the program will read `vocab.json` `merges.txt` `config.json` `model.safetensors`
